import { GameValues } from '@common/interfaces/game-play/game-values';
import { TimeConcept } from '@common/interfaces/general/time-concept';

export const THREE_SECONDS: TimeConcept = { seconds: 3, minutes: 0 };

export const GAME_VALUES: GameValues = {
    timerTime: 30,
    penaltyTime: 5,
    gainedTime: 5,
};
