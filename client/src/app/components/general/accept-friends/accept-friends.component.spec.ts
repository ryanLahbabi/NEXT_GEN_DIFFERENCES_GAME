/* eslint-disable */

describe('AcceptFriendsComponent', () => {
    // let component: AcceptFriendsComponent;
    // let fixture: ComponentFixture<AcceptFriendsComponent>;

    // beforeEach(async () => {
    //     await TestBed.configureTestingModule({
    //         imports: [RouterTestingModule],
    //         declarations: [AcceptFriendsComponent],
    //         providers: [HttpClient, HttpHandler, UrlSerializer, ChildrenOutletContexts],
    //         schemas: [CUSTOM_ELEMENTS_SCHEMA],
    //     }).compileComponents();

    //     fixture = TestBed.createComponent(AcceptFriendsComponent);
    //     component = fixture.componentInstance;
    //     fixture.detectChanges();
    // });

    it('should create', () => {
        expect(true).toBeTruthy();
    });
});
